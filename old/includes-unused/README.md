# Archived unused includes

These include templates were moved from `_includes/` after a reference audit.

## Why archived

They had no include-tag references in source files (excluding `old/` and generated `_site/`).

## Restore procedure

If a page later needs one of these templates, move it back into `_includes/` with the same filename.
